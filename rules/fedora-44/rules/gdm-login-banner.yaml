id: fedora44-gdm-login-banner

benchmark:
  name: CIS Red Hat Enterprise Linux 9 Benchmark
  version: 2.0.0
  item: 1.8.2
  section: Initial Setup > Configure GNOME Display Manager
  title:
    pl: Ekran logowania GDM pokazuje ostrzeżenie
    en: Ensure GDM login banner is configured
  level: 1
  profile: workstation

platform:
  system: fedora-44

risk_category: low
disruption: needs-review
assessment_state: assessed-impact-described

description:
  pl: |
    Ekran logowania GNOME (GDM) nie korzysta z /etc/issue, więc ostrzeżenie
    ustawione dla konsoli tekstowej nie pojawia się przy logowaniu
    graficznym. GDM ma własne ustawienie: po włączeniu pokazuje wskazany
    tekst nad polem hasła. Ostrzeżenie informuje, że dostęp mają tylko osoby
    upoważnione, co ułatwia późniejsze dochodzenie roszczeń wobec intruza.

    Punkt wymaga, żeby ostrzeżenie było włączone i miało treść. Domyślnie
    jest wyłączone.
  en: |
    The GNOME login screen (GDM) does not use /etc/issue, so a warning set for
    the text console does not appear at graphical sign-in. GDM has its own
    setting: when turned on, it shows the given text above the password field.
    The warning says that only authorized persons have access, which makes it
    easier to pursue claims against an intruder later.

    The item requires the warning to be turned on and to have text. By default
    it is turned off.

disruption_description:
  pl: |
    Na ekranie logowania pojawia się tekst ostrzeżenia. Logowanie działa jak
    wcześniej, bez dodatkowego potwierdzania. Zmiana działa od następnego
    uruchomienia ekranu logowania, czyli po wylogowaniu lub restarcie.
  en: |
    The warning text appears on the login screen. Sign-in works as before,
    with no extra confirmation. The change takes effect the next time the
    login screen starts, that is after signing out or a restart.

implementation_consequences:
  impact_areas:
    - user-interface
  what_breaks:
    pl:
      - Ekran logowania pokazuje ostrzeżenie, które zajmuje miejsce nad polem hasła
    en:
      - The login screen shows a warning that takes up space above the password field
  interruption: logoff
  user_visible: true
  reversibility: immediate

audit:
  shell: bash
  test_command: |
    if ! command -v gsettings >/dev/null 2>&1; then echo GDM_ABSENT; exit 0; fi
    d=$(mktemp -d) || { echo CHECK_ERROR; exit 0; }
    trap 'rm -rf "$d"' EXIT
    g() { env -i PATH=/usr/bin:/bin HOME="$d" XDG_CONFIG_HOME="$d" XDG_RUNTIME_DIR="$d" DCONF_PROFILE=gdm GSETTINGS_BACKEND=dconf gsettings "$@" 2>/dev/null; }
    if ! g list-schemas | grep -qx org.gnome.login-screen; then echo GDM_ABSENT; exit 0; fi
    state() {
      e=$(g get org.gnome.login-screen banner-message-enable) && m=$(g get org.gnome.login-screen banner-message-text) || { echo CHECK_ERROR; return; }
      if [ "$m" = "''" ]; then m=empty; else m=set; fi
      if [ "$e" = true ] && [ "$m" = set ]; then echo BANNER_ON; else echo "BANNER_OFF:enable=$e,text=$m"; fi
    }
    state
  expected: '^(BANNER_ON|GDM_ABSENT)$'
  comparison: regex

remediation:
  shell: bash
  backup_command: |
    (
      set -e
      f=/etc/dconf/db/gdm.d/sentin-harden-login-banner
      D=/etc/dconf/db/gdm.d
      b='{{backup_file}}'
      mkdir -p "$(dirname "$b")"
      {
        echo format=dconf-1
        m=""
        p=$(dirname "$f")
        while [ "$p" != /etc/dconf/db ]; do
          if [ ! -d "$p" ]; then m="$m $p"; fi
          p=$(dirname "$p")
        done
        echo "missing=${m# }"
        if [ -e /etc/dconf/db/gdm ]; then echo db=1; else echo db=0; fi
        if [ -f "$f" ]; then echo file=1; else echo file=0; fi
        echo ---
        if [ -f "$f" ]; then cat "$f"; fi
      } > "$b"
      echo "Saved previous state to $b"
    )

  change_command: |
    (
      set -e
      f=/etc/dconf/db/gdm.d/sentin-harden-login-banner
      D=/etc/dconf/db/gdm.d
      b='{{backup_file}}'
      [ -r "$b" ] || { echo "Backup file not found: $b" >&2; exit 1; }
      d=$(mktemp -d) || exit 1
      trap 'rm -rf "$d"' EXIT
      g() { env -i PATH=/usr/bin:/bin HOME="$d" XDG_CONFIG_HOME="$d" XDG_RUNTIME_DIR="$d" DCONF_PROFILE=gdm GSETTINGS_BACKEND=dconf gsettings "$@" 2>/dev/null; }
      if ! command -v gsettings >/dev/null 2>&1 || ! g list-schemas | grep -qx org.gnome.login-screen; then
        echo "GDM is not installed, nothing to change"; exit 0
      fi
      if [ ! -d "$D" ] && [ -e /etc/dconf/db/gdm ]; then
        echo "/etc/dconf/db/gdm is not built from keyfiles in $D, not changing it" >&2; exit 1
      fi
      state() {
        e=$(g get org.gnome.login-screen banner-message-enable) && m=$(g get org.gnome.login-screen banner-message-text) || { echo CHECK_ERROR; return; }
        if [ "$m" = "''" ]; then m=empty; else m=set; fi
        if [ "$e" = true ] && [ "$m" = set ]; then echo BANNER_ON; else echo "BANNER_OFF:enable=$e,text=$m"; fi
      }
      body() {
        cur=$(g get org.gnome.login-screen banner-message-text)
        if [ "$cur" = "''" ]; then cur="'This system is for authorized users only. All activity may be monitored and recorded.'"; fi
        printf '%s\n' '[org/gnome/login-screen]' 'banner-message-enable=true' "banner-message-text=$cur"
      }
      mkdir -p "$(dirname "$f")"
      tmp=$(mktemp "$f.XXXXXX")
      { echo '# Sentin-Harden: fedora44-gdm-login-banner'; body; } > "$tmp"
      chmod 0644 "$tmp"
      mv -f "$tmp" "$f"
      if command -v restorecon >/dev/null 2>&1; then restorecon "$f"; fi
      dconf update
      s=$(state)
      if ! printf '%s\n' "$s" | grep -Eq '^(BANNER_ON)$'; then
        echo "The setting is still not in effect ($s): another dconf database or file overrides it" >&2; exit 1
      fi
      echo "Wrote $f and updated the dconf databases ($s)"
    )

  rollback_command: |
    (
      set -e
      f=/etc/dconf/db/gdm.d/sentin-harden-login-banner
      D=/etc/dconf/db/gdm.d
      b='{{backup_file}}'
      [ -r "$b" ] || { echo "Backup file not found: $b" >&2; exit 1; }
      [ "$(head -n 1 "$b")" = format=dconf-1 ] || { echo "Backup file is damaged: $b" >&2; exit 1; }
      m=$(sed -n '2s/^missing=//p' "$b")
      db=$(sed -n '3s/^db=//p' "$b")
      fl=$(sed -n '4s/^file=//p' "$b")
      case "$db$fl" in [01][01]) ;; *) echo "Backup file is damaged: $b" >&2; exit 1 ;; esac
      if [ "$fl" = 1 ]; then
        tmp=$(mktemp "$f.XXXXXX")
        sed '1,/^---$/d' "$b" > "$tmp"
        chmod 0644 "$tmp"
        mv -f "$tmp" "$f"
        if command -v restorecon >/dev/null 2>&1; then restorecon "$f"; fi
      else
        rm -f "$f"
      fi
      for p in $m; do rmdir "$p" 2>/dev/null || true; done
      if command -v dconf >/dev/null 2>&1; then dconf update; fi
      if [ "$db" = 0 ] && [ ! -d "$D" ]; then rm -f /etc/dconf/db/gdm; fi
      echo "Restored the previous state of $f and updated the dconf databases"
    )

privileges: root
runnable: true

sources:
  - https://help.gnome.org/admin/system-admin-guide/stable/login-banner.html.en
  - https://help.gnome.org/admin/system-admin-guide/stable/dconf-profiles.html.en

notes:
  pl: |
    Jeśli tekst ostrzeżenia jest już ustawiony, naprawa go zostawia i tylko
    włącza wyświetlanie. W przeciwnym razie wpisuje neutralny tekst:
    "This system is for authorized users only. All activity may be monitored and recorded.", który warto zastąpić tekstem uzgodnionym
    w organizacji.

    Sprawdzenie odczytuje ustawienia przez gsettings z profilu dconf gdm,
    z pustym katalogiem domowym, czyli tak, jak zobaczy je ekran logowania bez własnych
    ustawień. Czyta skompilowane bazy w /etc/dconf/db, więc plik zapisany
    w katalogu .d bez uruchomienia dconf update nie spełnia punktu. Jeśli GDM
    nie jest zainstalowany, wynik GDM_ABSENT oznacza, że punkt nie dotyczy
    systemu.

    Naprawa zapisuje własny plik /etc/dconf/db/gdm.d/sentin-harden-login-banner i uruchamia dconf update, które
    przebudowuje wszystkie bazy z plików w katalogach .d. Potem sprawdza
    ustawienie ponownie i kończy się błędem, jeśli inna baza lub plik je
    nadpisuje. Wycofanie przywraca poprzedni stan pliku i ponownie uruchamia
    dconf update. Profilu /usr/share/dconf/profile/gdm z pakietu gdm naprawa
    nie kopiuje do /etc, bo kopia bez wpisu file-db pominęłaby domyślne
    ustawienia ekranu logowania.
  en: |
    If the warning text is already set, the remediation keeps it and only
    turns the display on. Otherwise it writes a neutral text: "This system is for authorized users only. All activity may be monitored and recorded.",
    which is worth replacing with text agreed in the organization.

    The check reads the settings with gsettings from the gdm dconf profile
    with an empty home directory, that is as the login screen without own settings
    sees them. It reads the compiled databases in /etc/dconf/db, so a file
    written to a .d directory without running dconf update does not meet the
    item. If GDM is not installed, the GDM_ABSENT result means the item does
    not apply to the system.

    The remediation writes its own /etc/dconf/db/gdm.d/sentin-harden-login-banner file and runs dconf update, which
    rebuilds all databases from the files in the .d directories. It then
    checks the setting again and fails if another database or file overrides
    it. Rollback restores the previous state of the file and runs dconf update
    again. The remediation does not copy the /usr/share/dconf/profile/gdm
    profile from the gdm package to /etc, because a copy without the file-db
    entry would skip the login screen defaults.
